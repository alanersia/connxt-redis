# Changelog

## 0.1.0

- Native TCP RESP2/RESP3 client, sync/async commands, pooling, TLS, Sentinel,
  transactions, pipelines, pub/sub, streams, scans, scripting, metrics, and
  Redis 6/7 integration fixtures.
- Cluster `MOVED`/`ASK` routing remains explicitly outside this release.
